<Page
    x:Class="客户端.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:客户端.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" KeyUp="Page_KeyUp">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" KeyUp="StackPanel_KeyUp">
            <TextBox  Name="accountBox" Margin="10" Header="请输入用户ID" />
            <PasswordBox  Name="passwordbox" Margin="10" Header="请输入密码" />
            <Button Margin="0,10,0,0" Content="登陆" 
                    HorizontalAlignment="Center" 
                    Click="Button_Click" />
        </StackPanel>
    </Grid>
</Page>
